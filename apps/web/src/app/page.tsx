import { SITE_NAME } from "@/site";

export default function Home() {
  return (
    <main>
      <h1>{SITE_NAME}</h1>
      <p>Public site — deployed via GitHub Pages. Scaffold only, no content yet.</p>
    </main>
  );
}
